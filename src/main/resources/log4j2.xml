<!-- <?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Properties>
        <!-- Runtime configurable log format -->
        <Property name="logPatternMinimal">%m%n</Property>
        <Property name="logPatternSimple">%d{yyyy-MM-dd HH:mm:ss} [%p] %m%n</Property>
        <Property name="logPatternVerbose">%d{yyyy-MM-dd HH:mm:ss} [%p] [%c{1.}] [%F:%L] %m%n</Property>
        <Property name="logPatternJson">{
            "timestamp": "%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}",
            "message": "%m",
            "level": "%p",
            "package": "%c{1.}",
            "file": "%F",
            "line": "%L",
            "traceId": "%X{traceId}",
            "stackTrace": "%ex{full}"
        }%n</Property>
        <!-- Default log level -->
        <Property name="defaultLogPattern">${sys:logPattern:-logPatternJson}</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${defaultLogPattern}" />
        </Console>
    </Appenders>

    <File name="FileLogger" fileName="logs/app.log" append="true">
        <PatternLayout pattern="${defaultLogPattern}" />
    </File>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console" />
            <AppenderRef ref="FileLogger" />
        </Root>
    </Loggers>
</Configuration> -->

